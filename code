fetch dt.davis.problems
| filter not(dt.davis.is_duplicate) and event.status == "ACTIVE"
| filter contains(event.name, "Transaction Log Used above 90 Percent")
| fields display_id, event.name, affected_entity_types, affected_entity_ids, entity_tags
