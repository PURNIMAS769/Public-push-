fetch dt.entity.service
| filter entity.name == "accumui"
| fields id, entity.name, tags

fetch dt.entity.service
| filter matchesPhrase(tags, "Accums")
| fields id, entity.name, tags

fetch dt.entity.host
| filter matchesPhrase(tags, "Accums")
| fields id, entity.name, tags

fetch dt.entity.custom_device
| filter matchesPhrase(tags, "Accums")
| fields id, entity.name, tags


fetch dt.entity.host
| filter matchesPhrase(tags, "6871")
| fields id, entity.name, tags

