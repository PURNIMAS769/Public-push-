(matching the outer field being filtered). Let's rename it to match exactly, removing any ambiguity.

Fixed — SMSEXEC
timeseries page_faults = avg(dt.host.memory.avail.pfps, scalar:true),
  by:{dt.entity.host},
  filter: dt.entity.host in [
    fetch dt.entity.process_group_instance
    | filter contains(entity.name, "smsexec.exe")
    | fieldsAdd dt.entity.host = runs_on[dt.entity.host]
    | fields dt.entity.host
  ]
| fieldsAdd hostname = entityName(dt.entity.host)
| fields hostname, page_faults
| sort page_faults desc

Fixed — WsusService
timeseries page_faults = avg(dt.host.memory.avail.pfps, scalar:true),
  by:{dt.entity.host},
  filter: dt.entity.host in [
    fetch dt.entity.process_group_instance
    | filter contains(entity.name, "WsusService.exe")
    | fieldsAdd dt.entity.host = runs_on[dt.entity.host]
    | fields dt.entity.host
  ]
| fieldsAdd hostname = entityName(dt.entity.host)
| fields hostname, page_faults
| sort page_faults desc

Fixed — SiteComp
timeseries page_faults = avg(dt.host.memory.avail.pfps, scalar:true),
  by:{dt.entity.host},
  filter: dt.entity.host in [
    fetch dt.entity.process_group_instance
    | filter contains(entity.name, "sitecomp.exe")
    | fieldsAdd dt.entity.host = runs_on[dt.entity.host]
    | fields dt.entity.host
  ]
| fieldsAdd hostname = entityName(dt.entity.host)
| fields hostname, page_faults
| sort page_faults desc
