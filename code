fetch dt.entity.host
| filter osType == "LINUX"
| fieldsAdd tag_list = tags
| expand tag_list
| fieldsAdd tag_key = if(contains(tag_list, ":"), splitString(tag_list, ":")[0], else: tag_list)
| summarize count(), by:{tag_key}
| sort tag_key asc
