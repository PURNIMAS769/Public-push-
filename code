timeseries sum(dt.service.request.count), sum(dt.service.request.failure_count), 
avg(dt.service.request.response_time), by:{dt.entity.service}, from:now()-7d
| filter dt.entity.service == "SERVICE-F65400876ACCE3AA"
