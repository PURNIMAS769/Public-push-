fetch dt.entity.host | filter cloudType <> "" OR hypervisorType == "VMWARE" 
| fieldsAdd cloudType | summarize by: {cloudType}, count()


fetch dt.entity.host
| fieldsAdd hypervisorType, cloudType
| summarize count(), by:{hypervisorType}


fetch dt.entity.host
| fieldsAdd clustered_by
| limit 5
