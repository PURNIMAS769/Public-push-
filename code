fetch dt.entity.host
| expand tags
| filter toString(tags) == "MongoDB_Supported"
| fieldsAdd host_id = id, host_name = entity.name
| lookup [
    timeseries read_iops = avg(dt.host.disk.read_ops), by:{dt.entity.host}, from:-2h
  ], sourceField:host_id, lookupField:dt.entity.host, prefix:"m_"
| fields host_name, read_iops = m_read_iops



fetch dt.entity.host
| expand tags
| filter toString(tags) == "MongoDB_Supported"
| fieldsAdd host_id = id, host_name = entity.name
| lookup [
    timeseries write_iops = avg(dt.host.disk.write_ops), by:{dt.entity.host}, from:-2h
  ], sourceField:host_id, lookupField:dt.entity.host, prefix:"m_"
| fields host_name, write_iops = m_write_iops


fetch dt.entity.host
| expand tags
| filter toString(tags) == "MongoDB_Supported"
| fieldsAdd host_id = id, host_name = entity.name
| lookup [
    timeseries read_ms = avg(dt.host.disk.read_time), by:{dt.entity.host}, from:-2h
  ], sourceField:host_id, lookupField:dt.entity.host, prefix:"r_"
| lookup [
    timeseries write_ms = avg(dt.host.disk.write_time), by:{dt.entity.host}, from:-2h
  ], sourceField:host_id, lookupField:dt.entity.host, prefix:"w_"
| fields host_name, read_latency_ms = r_read_ms, write_latency_ms = w_write_ms
