Diagnostic — raw data first, no entityName(), no rounding
timeseries drop_rx = avg(dt.host.net.nic.packets.dropped.rx),
  by:{dt.entity.host, dt.entity.network_interface},
  filter: dt.entity.host in [
    fetch dt.entity.host
    | filter arrayIndexOf(tags, "APMID:APP00005497") != -1
    | fields id
  ]
| fields dt.entity.host, dt.entity.network_interface, drop_rx
| limit 20

f that returns rows — find the correct entity type name for the NIC dimension
fetch metric.series
| filter metric.key == "dt.host.net.nic.packets.dropped.rx"
| fields metric.key, dimensions
| limit 5
