timeseries restarts = sum(dt.kubernetes.container.restarts),
by:{k8s.namespace.name},
from:-7d
| filter k8s.namespace.name == "app00002734-paperless-inventory"
| fields timeframe, interval, k8s.namespace.name, restarts
