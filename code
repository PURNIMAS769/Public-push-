| fieldsAdd rt_ms = round(arrayAvg(rt) / 1000, decimals: 2) 
| fieldsAdd `Response Time` = if(rt_ms >= 1000, concat(toString(round(rt_ms / 1000, decimals: 2)), " s"), 
else: concat(toString(rt_ms), " ms"))
