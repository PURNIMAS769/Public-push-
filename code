fetch spans
| filter dt.entity.service == "SERVICE-DD6661CA37FE7FD4"
| filter span.kind == "server"
| summarize count(), by:{dt.entity.service, trace.parent.span.id}
| limit 10

fetch dt.entity.service
| filter id == "SERVICE-DD6661CA37FE7FD4"
| fieldsAdd entity.name, calls


fetch dt.entity.service
| filter id == "SERVICE-DD6661CA37FE7FD4"
| fieldsAdd entity.name, fromRelationships
