fetch dt.entity.process_group_instance
| filter (contains(toString(softwareTechnologies), "Lettuce") or contains(toString(softwareTechnologies), "Redis"))
| filter contains(toString(tags), "APMID:APP00006805")
| fieldsAdd entity.name, id, tags


fetch dt.entity.process_group_instance
| filter (contains(toString(softwareTechnologies), "Lettuce") or contains(toString(softwareTechnologies), "Redis"))
| filter contains(toString(tags), "006805")
| fieldsAdd entity.name, id, tags
