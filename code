
timeseries role = avg(`sql-server.always-on.ar.role`),
  by:{`availability.group.name`, `availability.replica.availability_mode`}
| fields `availability.group.name`, `availability.replica.availability_mode`, role
| limit 20



timeseries role = avg(`sql-server.always-on.ar.role`),
  by:{`availability.group.name`}
| fields `availability.group.name`, role
| limit 20



timeseries role = avg(`sql-server.always-on.ar.role`),
  by:{`availability.replica.server_name`}
| fields `availability.replica.server_name`, role
| limit 20
