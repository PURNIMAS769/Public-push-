timeseries physical_reads = sum(db2.bp.total_physical_reads.count),
by:{`dt.entity.sql:db2_instance`}, from:-2h, interval:5m
| fieldsAdd instance = entityName(`dt.entity.sql:db2_instance`)
| filter isNotNull(instance)
| fields timeframe, interval, instance, physical_reads
