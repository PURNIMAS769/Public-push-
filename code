Don't assume the tag key. Run this to see the actual tag key/value format on your applications:

fetch dt.entity.application
| fieldsAdd tags
| filter matchesValue(tags, "*Claims*")
| fields entity.name, id, tags
| limit 20


This confirms whether it's Por
