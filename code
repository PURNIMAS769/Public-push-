timeseries avg(dt.osservice.availability), by:{dt.entity.service}, from:-2h
| fieldsAdd serviceName = entityName(dt.entity.service)
| filter arrayIndexOf(tags, "APMID:APP00006805") > -1


timeseries avg(dt.osservice.availability), by:{dt.entity.service}, from:-2h
