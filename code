fetch dt.entity.host
| expand tags
| filter toString(tags) == "MongoDB_Supported"
| fieldsAdd host_id = id, host_name = entity.name
| lookup [
    timeseries disk = avg(dt.host.disk.used.percent, scalar:true), by:{dt.entity.host}, from:-2h
  ], sourceField:host_id, lookupField:dt.entity.host, prefix:"m_"
| fields host_name, disk_used_pct = round(m_disk, decimals:2)
