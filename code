Discovery Gaps Still Open
1. Process Group / Process Group Instance detail


fetch dt.entity.process_group_instance
| filter arrayIndexOf(tags, "APMID:APP00005754") > -1
   or in(id, array("PROCESS_GROUP_INSTANCE-808C2A6695794053C","PROCESS_GROUP_INSTANCE-C2AEA7C7D8153E3F","PROCESS_GROUP_INSTANCE-2ED0CCA279AE1E6A"))
| fieldsAdd pg = belongs_to[dt.entity.process_group]
| fields id, entity.name, pg, tags

2. Pod/Container-level entities (cloud_application_instance)


fetch dt.entity.cloud_application_instance
| filter belongs_to[dt.entity.cloud_application] in 
   array("CLOUD_APPLICATION-D0670D42085AE8D3","CLOUD_APPLICATION-C4ECA6048A8600DA")
| fields id, entity.name, belongs_to[dt.entity.cloud_application]

3. Kubernetes Cluster confirmation


fetch dt.entity.kubernetes_cluster
| fieldsAdd ns = belongs_to[dt.entity.cloud_application_namespace]
| filter arrayIndexOf(ns, "CLOUD_APPLICATION_NAMESPACE-6AE19D0D570B27B8") > -1
   or arrayIndexOf(ns, "CLOUD_APPLICATION_NAMESPACE-07FD15EE71CD9460") > -1
| fields id, entity.name, ns

4. Real dependency validation (MuleSoft / WebSphere MQ / ForgeRock SSO)


fetch dt.entity.service
| filter id == "SERVICE-2FCF304C7277A39A"
| fieldsAdd calls = calls[dt.entity.service]
| fieldsAdd calledBy = called_by[dt.entity.service]
| fields id, entity.name, calls, calledBy



5. Active Problems / Davis AI scoping


fetch dt.davis.problems
| filter not(dt.davis.is_duplicate)
| expand affected_entity_ids
| filter affected_entity_ids in array("SERVICE-10608C35AFE20E11","SERVICE-2FCF304C7277A39A","SERVICE-CC2B24165FF39D16","HOST-4BDF46C5E70ECD3B","HOST-B4555E0094E2A634","HOST-3AE95EA3AFC4E363","HOST-E1093B67544CB72F")
| fields display_id, event.category, event.start, event.end, affected_entity_ids

6. Change/Deployment Events (for Change Intelligence section)
fetch events
| filter event.kind == "CUSTOM_DEPLOYMENT" or event.type == "DEPLOYMENT_EVENT"
| filter arrayIndexOf(affected_entity_ids, "SERVICE-2FCF304C7277A39A") > -1
| fields event.name, event.start, affected_entity_ids

7. Metric availability check — Process CPU/Memory


timeseries avg(dt.process.cpu.usage), by:{dt.entity.process_group_instance}, filter: in(dt.entity.process_group_instance, array("PROCESS_GROUP_INSTANCE-808C2A6695794053C","PROCESS_GROUP_INSTANCE-C2AEA7C7D8153E3F"))

timeseries avg(dt.process.memory.usage), by:{dt.entity.process_group_instance}, filter: in(dt.entity.process_group_instance, array("PROCESS_GROUP_INSTANCE-808C2A6695794053C","PROCESS_GROUP_INSTANCE-C2AEA7C7D8153E3F"))

8. Container-level metric check (OpenShift section)
timeseries avg(dt.kubernetes.container.cpu.usage), by:{k8s.namespace.name}, filter: k8s.namespace.name == "app00005754-blue-chip-duplicate-check"

timeseries avg(dt.kubernetes.container.memory.working_set), by:{k8s.namespace.name}, filter: k8s.namespace.name == "app00005754-blue-chip-duplicate-check"

9. Host disk/network entity presence (Infra section)
fetch dt.entity.disk
| filter arrayIndexOf(belongs_to[dt.entity.host], "HOST-4BDF46C5E70ECD3B") > -1
   or arrayIndexOf(belongs_to[dt.entity.host], "HOST-B4555E0094E2A634") > -1
| fields id, entity.name



fetch dt.entity.service
| filter id == "SERVICE-D5CC8D6733743AB0"
| fieldsAdd calledBy = called_by[dt.entity.service]
| fields id, entity.name, calledBy

11. Non-Prod environment (Test/Dev) — do we need it too?


fetch dt.entity.service
| filter arrayIndexOf(tags, "APMID:APP00005754") > -1
| fields id, entity.name, tags
| filter contains(tags, "test", caseSensitive:false) or contains(tags, "non-prod", caseSensitive:false)
