fetch dt.entity.host
| filter matchesPhrase(tags, "Blue_Chip_Benefit_Determination_Dev")
| fields id, entity.name, tags


fetch dt.entity.host
| filter matchesPhrase(tags, "Blue_Chip_Benefit_Determination_Test")
| fields id, entity.name, tags

