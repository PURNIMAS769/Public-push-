fetch metric.series
| filter contains(metric.key, "dt.host.net")
| summarize count(), by: {metric.key}


fetch metric.series
| filter contains(metric.key, "mysql") and (contains(metric.key, "network") or contains(metric.key, "connection") or contains(metric.key, "bytes"))
| summarize count(), by: {metric.key}
