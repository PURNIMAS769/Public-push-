fetch metric.series
| filter metric.key == "service.mule_gw_volumetrics_claimmod"
| limit 5


fetch metric.series
| filter metric.key == "service.mule_gw_volumetrics_claim"
| limit 5


timeseries sum(service.mule_gw_volumetrics_claimmod), from:now()-7d


fetch metric.series
| filter metric.key == "service.mule_gw_volumetrics_claimmod" or metric.key == "service.mule_gw_volumetrics_claim"
| fieldsAdd allTags = toString(dimensions)
| filter contains(allTags, "APP00006805") or contains(allTags, "accums") or contains(allTags, "Accum")


