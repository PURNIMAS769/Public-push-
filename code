timeseries diskfree_pct = avg(dt.host.disk.free), by: { dt.entity.host, dt.entity.disk }, from:-30d
| fieldsAdd host_name = entityName(dt.entity.host)
| fieldsAdd disk_name = entityName(dt.entity.disk)
| fieldsAdd host_tags = entityAttr(dt.entity.host, "tags")
| filter arrayIndexOf(host_tags, "APMID:APP00002734") > -1
| fieldsAdd disk_usage_pct = 100 - diskfree_pct[]
| fields timeframe, interval, host_name, disk_name, disk_usage_pct, dt.entity.host, dt.entity.disk




-----------------------------------------------
timeseries {
    used_memory = avg(dt.kubernetes.resourcequota.requests_memory_used),
    max_memory = avg(dt.kubernetes.resourcequota.requests_memory)
},
by: {k8s.cluster.name, k8s.namespace.name, k8s.resourcequota.name},
from: -30d
| filter k8s.namespace.name == "app00002734-paperless-inventory"
| fieldsAdd memory_used_percent = 100.0 * used_memory[] / max_memory[]
| fields timeframe, interval, k8s.cluster.name, k8s.namespace.name, k8s.resourcequota.name, memory_used_percent




-----------------------------------------------------


timeseries requests = sum(dt.service.request.count),
by:{dt.entity.service},
from:-30d
| filter in(dt.entity.service, classicEntitySelector("type(service),tag(\"APMID:APP00002734\")"))
| fieldsAdd service_name = entityName(dt.entity.service)
| fieldsAdd total_check = arraySum(requests)
| filter isNotNull(total_check) and total_check > 0
| fields timeframe, interval, service_name, requests

---------------------------------
timeseries requests = sum(dt.service.request.count),
by:{dt.entity.service},
from:-30d
| filter in(dt.entity.service, classicEntitySelector("type(service),tag(\"APMID:APP00002734\")"))
| fieldsAdd service_name = entityName(dt.entity.service)
| fieldsAdd total_check = arraySum(requests)
| filter isNotNull(total_check) and total_check > 0
| fields timeframe, interval, dt.entity.service, service_name, requests
----------------------------------------------------------

timeseries deadlocks = avg(`sql-server.locks.deadlocks.count`), by:{device.name}, from:-30d
| filter contains(device.name, "wpisq")
| fields timeframe, interval, device.name, deadlocks
