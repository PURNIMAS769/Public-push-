timeseries active_conns = avg(db2.active_connections),
by:{`dt.entity.sql:db2_instance`}
| fieldsAdd instance = entityName(`dt.entity.sql:db2_instance`)
| filter isNotNull(instance)
| fields timeframe, interval, instance, active_conns
