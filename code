fetch dt.entity.custom_device
| fieldsAdd entity.type
| summarize count(), by:{entity.type}
| sort count() desc


fetch dt.entity.host
| fieldsAdd hostGroupName, tags
| filter contains(lower(hostGroupName), "nutanix") or contains(lower(toString(tags)), "nutanix")
| fields id, entity.name, hostGroupName, tags
