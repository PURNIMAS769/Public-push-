fetch spans
| filter server.address == "dp0chlfg.hcscint.net"
| filter dt.entity.service == "SERVICE-DD6661CA37FE7FD4" or dt.entity.service == "SERVICE-A2463600558742E8"
| summarize count(), by:{dt.entity.service, span.name}
