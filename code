fetch dt.entity.process_group_instance
| filter contains(entity.name, "mongod") or contains(entity.name, "mongo")
| fields entity.name, id
| limit 50


fetch metric.series
| filter contains(metric.key, "dt.process.cpu") or contains(metric.key, "dt.process.memory")
| summarize count(), by:{metric.key}
