Tile 1 — Bits In (trend, top 20 by avg)

timeseries bits_in = avg(cloud.azure.microsoft_network.expressroutecircuits.bitsinpersecond),
  by:{dt.entity.custom_device}
| fieldsAdd circuit_name = entityName(dt.entity.custom_device)
| sort arrayAvg(bits_in) desc
| limit 20


Tile 2 — Bits Out (trend, top 20 by avg)

timeseries bits_out = avg(cloud.azure.microsoft_network.expressroutecircuits.bitsoutpersecond),
  by:{dt.entity.custom_device}
| fieldsAdd circuit_name = entityName(dt.entity.custom_device)
| sort arrayAvg(bits_out) desc
| limit 20
